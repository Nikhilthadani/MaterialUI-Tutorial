import React from "react";
import { Typography } from "@mui/material";
const Test = () => {
  return (
    <div>
      <Typography variant="h5" sx={{ color: "red" }}>
        Hello World
      </Typography>
    </div>
  );
};

export default Test;
